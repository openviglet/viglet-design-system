/** The console page vocabulary and each export's bento replacement. */
export declare const CONSOLE_ERA: Readonly<Record<string, string | null>>

/** Exports of the router barrel the bento layer uses itself. */
export declare const ERA_NEUTRAL: readonly string[]
