{
  "description": "Deny a write that declares a component @viglet/viglet-design-system already exports, and name the import that replaces it.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/duplicate-guard.mjs\"",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
