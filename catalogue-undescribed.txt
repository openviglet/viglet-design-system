# Components with no doc comment yet (VDS150).
#
# dist/catalogue.json takes a component's purpose from the doc comment on its own
# declaration and invents none, so a name below is one find_component can match
# only by its name and props. scripts/check-catalogue.mjs fails the build when a
# component outside this list has no doc comment, and when one on it has gained a
# doc comment and was not removed here.
#
# Every exported component is described, so the list is empty and stays that way:
# a new component arrives with its doc comment, the first sentence its purpose.
